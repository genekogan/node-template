simple node.js template with Express and Sockets.io.  `npm install` to get the npm modules, then run.

	node app.js
	
then go over to `localhost:5000`. test sending data from client to server and vice-versa via the links.